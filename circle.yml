machine:
  php:
    version: 5.6.14
  node:
    version: 0.10.34

test:
  override:
    - make lint

deployment:
  next:
    branch: master
    commands:
      - git fetch --unshallow
      - git push -f ssh://56796a342d527194e200016a@profile-pinemz2.rhcloud.com/~/git/profile.git/ master:master
